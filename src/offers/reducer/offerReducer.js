// @flow

// import actions

const initialState = {
  fetching: false,
  fetched: false,
  offers: [],
};

export default function reducer(state: any = initialState, action: any) {
  switch (action.type) {

    default:
      return state;
  }
}