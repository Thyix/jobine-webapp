// @flow

// import offers actions and messages

export const fetchActivities = () => {
  return async (dispatch: Function) => {
  };
};

export const fetchOffersHistory = () => {
  return async (dispatch: Function) => {
  };
};